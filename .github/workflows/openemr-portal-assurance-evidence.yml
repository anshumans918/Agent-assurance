name: OpenEMR Patient Portal · Assurance & Evidence

# Requirements -> designed tests -> executed proof -> coverage gate.
# Every stage is kane-cli; nothing is hand-authored.

on:
  push:
    branches: [main]
    paths:
      - 'requirements/**'
      - '.testmuai/tests/**'
      - 'test-data/**'
      - 'scripts/**'
      - '.github/workflows/openemr-portal-assurance-evidence.yml'
  pull_request:
    paths:
      - 'requirements/**'
      - '.testmuai/tests/**'
      - 'test-data/**'
      - 'scripts/**'
  schedule:
    # The public demo resets at ~08:00 UTC daily; run after it has settled.
    - cron: '30 9 * * 1-5'
  workflow_dispatch:
    inputs:
      max_pairs:
        description: Max scenario+test pairs designed per use-case
        default: '8'
      coverage_threshold:
        description: Minimum proven coverage %% before the build fails
        default: '80'
      force_design:
        description: Redesign use-cases that already have a live design
        type: boolean
        default: false
      force_usecases:
        description: Use-case ids to redesign even if already designed (e.g. "uc-3")
        default: ''

# One run at a time across the whole repo, not per ref: every run signs in to the same
# two shared demo accounts, and a second login ends the first session (PRD BR-005).
concurrency:
  group: openemr-portal-assurance
  cancel-in-progress: false

permissions:
  contents: read
  pull-requests: write

env:
  APP_URL: https://demo.openemr.io/openemr/portal
  KANE_CLI_USER_AGENT: github-actions
  MAX_PAIRS: ${{ inputs.max_pairs || '8' }}
  COVERAGE_THRESHOLD: ${{ inputs.coverage_threshold || '80' }}

jobs:
  # ===========================================================================
  # 1 — ASSURANCE. The PRD is the input; a context graph and a set of designed
  #     *_test.md files are the output.
  # ===========================================================================
  assurance:
    name: 1 · Assurance — requirements to designed tests
    runs-on: ubuntu-latest
    # Designing the PRD's 11 use-cases took ~50 minutes at max_pairs 8 on run 1; a full
    # redesign at 12 plus retries needs more.
    timeout-minutes: 150
    outputs:
      usecase_count: ${{ steps.design.outputs.usecase_count }}
      test_count: ${{ steps.design.outputs.test_count }}
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-kane
        with:
          username: ${{ secrets.LT_USERNAME }}
          access-key: ${{ secrets.LT_ACCESS_KEY }}
          project-id: ${{ secrets.TESTMUAI_PROJECT_ID }}
          folder-id: ${{ secrets.TESTMUAI_FOLDER_ID }}

      # Bumped (v2, v3) whenever the PRD changes enough to need a clean graph: kane-cli
      # 0.8.13 cannot re-extract changed criteria over an existing graph
      # (CRITERIA_OPS_UNSUPPORTED: updating criteria needs an extract@5+ binding).
      # The graph is an append-only commit chain. Reusing it across runs means
      # unchanged requirements are not re-extracted and not re-billed.
      # .kane-state holds the designed *_test.md files (see scripts/assurance.sh):
      # kane-cli cannot rebuild them from the graph, so they travel with it.
      - name: Restore context graph
        uses: actions/cache/restore@v4
        with:
          path: |
            .context
            .kane-state
          key: kane-assurance-v3-${{ hashFiles('requirements/**') }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            kane-assurance-v3-${{ hashFiles('requirements/**') }}-
            kane-assurance-v3-

      - name: Point kane-cli at the portal
        run: kane-cli config set-url "$APP_URL"

      # Provisioned before design, not just before execution: kane-cli design reuses a
      # variable name that already exists in a pool file instead of inventing one, so the
      # designed tests come back using patient1_username etc. Design reads names only,
      # never values, and .testmuai/variables/ is never uploaded or cached.
      - name: Provision test data
        env:
          OPENEMR_PORTAL_USER: ${{ secrets.OPENEMR_PORTAL_USER }}
          OPENEMR_PORTAL_PASSWORD: ${{ secrets.OPENEMR_PORTAL_PASSWORD }}
          OPENEMR_PORTAL_EMAIL: ${{ secrets.OPENEMR_PORTAL_EMAIL }}
          OPENEMR_PORTAL_USER_2: ${{ secrets.OPENEMR_PORTAL_USER_2 }}
          OPENEMR_PORTAL_PASSWORD_2: ${{ secrets.OPENEMR_PORTAL_PASSWORD_2 }}
          OPENEMR_PORTAL_EMAIL_2: ${{ secrets.OPENEMR_PORTAL_EMAIL_2 }}
        run: python3 scripts/test_data.py provision

      - name: Ingest, extract, review, design
        id: design
        env:
          REQ_GLOB: requirements/*.md
          MAX_PAIRS: ${{ env.MAX_PAIRS }}
          AUTO_APPROVE: 'true'
          FORCE_DESIGN: ${{ inputs.force_design || 'false' }}
          FORCE_USECASES: ${{ inputs.force_usecases || '' }}
        run: bash scripts/assurance.sh

      - name: Save context graph
        if: ${{ !cancelled() && (steps.design.outcome == 'success' || steps.design.outcome == 'failure') }}
        uses: actions/cache/save@v4
        with:
          path: |
            .context
            .kane-state
          key: kane-assurance-v3-${{ hashFiles('requirements/**') }}-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Assurance summary
        if: always()
        run: |
          {
            echo "## 1 · Assurance"
            echo
            echo "Source of truth: \`requirements/openemr-patient-portal-prd.md\`"
            echo
            echo "| | |"
            echo "|---|---|"
            echo "| Live use-cases | ${{ steps.design.outputs.usecase_count || 0 }} |"
            echo "| Designed tests | ${{ steps.design.outputs.test_count || 0 }} |"
            echo "| Failed to design | ${{ steps.design.outputs.failed_usecases || 'none' }} |"
            echo
            echo '<details><summary>Context graph nodes</summary>'
            echo
            echo '```'
            kane-cli context list || true
            echo '```'
            echo
            echo '</details>'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Publish the graph and the designed tests
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: assurance-graph-${{ github.run_number }}
          path: |
            .context/
            .testmuai/tests/
            usecases.txt
          include-hidden-files: true
          retention-days: 30
          if-no-files-found: warn

  # ===========================================================================
  # 2 — EVIDENCE. Run every designed test as ONE execution so the whole suite
  #     seals into a single evidence pack.
  # ===========================================================================
  evidence:
    name: 2 · Evidence — execute and seal the pack
    needs: assurance
    runs-on: ubuntu-latest
    # Suite (~2.3 h) + retry pass + up to 150 min waiting out the demo reset; GitHub's
    # hosted-runner cap is 360.
    timeout-minutes: 355
    outputs:
      pack: ${{ steps.pack.outputs.path }}
      status: ${{ steps.execute.outputs.status }}
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-kane
        with:
          username: ${{ secrets.LT_USERNAME }}
          access-key: ${{ secrets.LT_ACCESS_KEY }}
          project-id: ${{ secrets.TESTMUAI_PROJECT_ID }}
          folder-id: ${{ secrets.TESTMUAI_FOLDER_ID }}

      - name: Fetch the designed tests
        uses: actions/download-artifact@v4
        with:
          name: assurance-graph-${{ github.run_number }}

      - name: Restore recorded steps
        uses: actions/cache/restore@v4
        with:
          path: .testmuai/tests/output-*
          key: kane-recordings-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: kane-recordings-

      # Nothing to start: the site under test is the public demo. Fail fast, before any
      # browser minute is spent, if it is down or mid-reset.
      - name: Check the demo portal is reachable
        run: |
          set -euo pipefail
          for i in $(seq 1 10); do
            if curl -sfL -o /dev/null "$APP_URL/index.php"; then
              echo "OpenEMR demo portal is up."
              exit 0
            fi
            sleep 15
          done
          echo "::error title=Demo unreachable::$APP_URL did not respond. The public demo may be resetting (~08:00 UTC) or down."
          exit 1

      - name: Provision test data
        env:
          OPENEMR_PORTAL_USER: ${{ secrets.OPENEMR_PORTAL_USER }}
          OPENEMR_PORTAL_PASSWORD: ${{ secrets.OPENEMR_PORTAL_PASSWORD }}
          OPENEMR_PORTAL_EMAIL: ${{ secrets.OPENEMR_PORTAL_EMAIL }}
          OPENEMR_PORTAL_USER_2: ${{ secrets.OPENEMR_PORTAL_USER_2 }}
          OPENEMR_PORTAL_PASSWORD_2: ${{ secrets.OPENEMR_PORTAL_PASSWORD_2 }}
          OPENEMR_PORTAL_EMAIL_2: ${{ secrets.OPENEMR_PORTAL_EMAIL_2 }}
        run: python3 scripts/test_data.py provision

      - name: Collect members
        id: members
        run: |
          set -euo pipefail
          find .testmuai/tests -name '*_test.md' | sort > members.txt
          echo "count=$(wc -l < members.txt | tr -d ' ')" >> "$GITHUB_OUTPUT"
          cat members.txt

      - name: Preflight
        run: |
          set -euo pipefail
          python3 scripts/test_data.py check members.txt
          mapfile -t TESTS < members.txt
          kane-cli testrun run "${TESTS[@]}" --dry-run \
            --name "OpenEMR portal preflight #${{ github.run_number }}"

      # The demo resets at ~08:00 UTC and returns 502 for ~20 minutes (run 4 lost 11 tests
      # to it). If the suite, at ~160 s per test, would overlap 07:45-08:30 UTC, wait until
      # 08:30 first, unless that wait is longer than 150 minutes, in which case run now and
      # let the retry pass pick up what the reset breaks.
      - name: Wait out the demo's daily reset
        env:
          MEMBER_COUNT: ${{ steps.members.outputs.count }}
        run: python3 scripts/reset_window.py wait "$MEMBER_COUNT"

      # --parallel 1 on purpose: every test signs in as one of two shared demo patients,
      # and a second login with the same account ends the first session (PRD BR-005),
      # so two workers would log each other out mid-test.
      - name: Execute the portal suite
        id: execute
        run: |
          set -uo pipefail
          set +e
          mapfile -t TESTS < members.txt
          run_suite() {
            kane-cli testrun run "${TESTS[@]}" \
              --name "OpenEMR portal regression #${{ github.run_number }}$1 (${GITHUB_SHA:0:7})" \
              --parallel 1 \
              --on-failure continue \
              --headless \
              | tee "$2"
            return "${PIPESTATUS[0]}"
          }
          run_suite "" testrun.ndjson
          CODE=$?
          # New members are authored one by one and no suite execution is sealed
          # (execution_id empty). Run the suite again to replay what was just recorded
          # as one execution, which is the pack coverage needs.
          if [ -z "$(python3 scripts/testrun_stream.py execution-id testrun.ndjson)" ] \
             && [ "$(python3 scripts/testrun_stream.py authored testrun.ndjson)" -gt 0 ]; then
            echo "Authoring pass only ($(python3 scripts/testrun_stream.py authored testrun.ndjson) member(s)); running the suite again as one execution."
            mv testrun.ndjson testrun-authoring.ndjson
            python3 scripts/reset_window.py wait "$(wc -l < members.txt)"
            run_suite "" testrun.ndjson
            CODE=$?
          fi
          echo "status=$CODE" >> "$GITHUB_OUTPUT"
          if [ "$CODE" -eq 2 ]; then exit 2; fi
          exit 0

      - name: Locate the first-pass pack
        id: pack1
        run: |
          set -euo pipefail
          EXEC_ID=$(python3 scripts/testrun_stream.py execution-id testrun.ndjson)
          PACK=".testmuai/evidence/${EXEC_ID}.evidence"
          if [ -z "$EXEC_ID" ] || [ ! -e "$PACK" ]; then
            echo "::error title=No suite pack::testrun sealed no suite execution (execution id '${EXEC_ID}'). Member packs are not a suite pack; see testrun.ndjson."
            exit 1
          fi
          echo "path=$PACK" >> "$GITHUB_OUTPUT"
          ls -lh "$PACK"

      # One more attempt for every member that did not pass, as a second execution.
      # Shared-account session loss, a slow dashboard and brief demo outages are
      # transient; a real defect fails twice. Skipped if it would run into the reset.
      - name: Retry failed members once
        id: retry
        run: |
          set -uo pipefail
          python3 scripts/testrun_stream.py failed testrun.ndjson > retry.txt
          COUNT=$(grep -c . retry.txt || true)
          echo "count=$COUNT" >> "$GITHUB_OUTPUT"
          if [ "$COUNT" -eq 0 ]; then echo "Nothing to retry."; exit 0; fi
          if ! python3 scripts/reset_window.py clear "$COUNT"; then
            echo "::warning title=Retry skipped::Retrying ${COUNT} member(s) now would overlap the demo reset."
            exit 0
          fi
          echo "Retrying ${COUNT} member(s)."
          set +e
          mapfile -t TESTS < retry.txt
          kane-cli testrun run "${TESTS[@]}" \
            --name "OpenEMR portal regression #${{ github.run_number }} retry (${GITHUB_SHA:0:7})" \
            --parallel 1 \
            --on-failure continue \
            --headless \
            | tee testrun-retry.ndjson
          echo "status=${PIPESTATUS[0]}" >> "$GITHUB_OUTPUT"
          exit 0

      # Coverage reads one pack, so the retry's pack is merged after the first pass's
      # (order-significant: the retry comes last). If the merge is refused, coverage
      # falls back to the first pass alone.
      - name: Combine the passes into one pack
        id: pack
        run: |
          set -uo pipefail
          FIRST="${{ steps.pack1.outputs.path }}"
          FINAL="$FIRST"
          if [ -s testrun-retry.ndjson ]; then
            RETRY_ID=$(python3 scripts/testrun_stream.py execution-id testrun-retry.ndjson)
            SECOND=".testmuai/evidence/${RETRY_ID}.evidence"
            if [ -n "$RETRY_ID" ] && [ -e "$SECOND" ]; then
              RUN_ID="openemr-${{ github.run_number }}-${{ github.run_attempt }}"
              if kane-cli evidence merge "$FIRST" "$SECOND" --run-id "$RUN_ID"; then
                FINAL=".testmuai/evidence/${RUN_ID}.evidence"
              else
                echo "::warning title=Merge refused::Could not merge the retry pack; coverage uses the first pass only."
              fi
            fi
          fi
          [ -e "$FINAL" ] || { echo "::error::Final pack $FINAL missing."; exit 1; }
          echo "path=$FINAL" >> "$GITHUB_OUTPUT"
          ls -lh "$FINAL"

      # testrun publishes the whole batch's evidence pack to the TMS dashboard via a
      # testrun_evidence_ingest event; absent means publish was skipped.
      - name: Report the dashboard link
        if: always() && steps.execute.outcome != 'skipped'
        env:
          TESTMUAI_PROJECT_ID: ${{ secrets.TESTMUAI_PROJECT_ID }}
        run: |
          {
            echo "## Dashboard"
            echo
            if grep -q '"type":"testrun_evidence_ingest"' testrun.ndjson 2>/dev/null; then
              echo "Evidence pack published — view every test's screenshots and steps directly:"
              echo
              echo "[Open in Test Manager](https://test-manager.lambdatest.com/projects/${TESTMUAI_PROJECT_ID})"
            else
              echo "⚠️ This run's evidence pack did not publish to the dashboard (no"
              echo "\`testrun_evidence_ingest\` event) — it's local-only this time. Download"
              echo "the \`openemr-evidence-*\` artifact and run \`kane-cli evidence serve\`"
              echo "to view it, or check \`kane-cli whoami\` / project-folder config."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Save recorded steps
        if: always() && steps.execute.outcome != 'skipped'
        uses: actions/cache/save@v4
        with:
          path: .testmuai/tests/output-*
          key: kane-recordings-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Publish the executed graph
        if: always() && steps.execute.outcome != 'skipped'
        uses: actions/upload-artifact@v4
        with:
          name: assurance-graph-executed-${{ github.run_number }}
          path: .context/
          include-hidden-files: true
          retention-days: 30
          if-no-files-found: error

      - name: Validate the pack
        run: |
          set -euo pipefail
          kane-cli evidence validate "${{ steps.pack.outputs.path }}" \
            --profile L1 --json | tee evidence-validation.json
          {
            echo "## 2 · Evidence"
            echo
            echo "Pack: \`$(basename "${{ steps.pack.outputs.path }}")\`  ·  L1 validated"
            echo
            echo "View it locally with:"
            echo
            echo '```bash'
            echo "kane-cli evidence serve $(basename "${{ steps.pack.outputs.path }}")"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Publish the evidence pack
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: openemr-evidence-${{ github.run_number }}
          # Only the sealed suite pack, at its full path: the coverage job reads it back
          # from exactly steps.pack.outputs.path.
          path: |
            ${{ steps.pack.outputs.path }}
            evidence-validation.json
            testrun.ndjson
            testrun-authoring.ndjson
            testrun-retry.ndjson
          include-hidden-files: true
          retention-days: 90
          if-no-files-found: warn

  # ===========================================================================
  # 3 — COVERAGE. Two axes: completeness owed by the graph, depth proven by the
  #     pack. This is the stage that turns "tests passed" into "requirements met".
  # ===========================================================================
  coverage:
    name: 3 · Coverage — designed vs proven
    needs: [assurance, evidence]
    if: always() && needs.evidence.outputs.pack != ''
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-kane
        with:
          username: ${{ secrets.LT_USERNAME }}
          access-key: ${{ secrets.LT_ACCESS_KEY }}
          project-id: ${{ secrets.TESTMUAI_PROJECT_ID }}
          folder-id: ${{ secrets.TESTMUAI_FOLDER_ID }}

      - name: Fetch the executed graph
        uses: actions/download-artifact@v4
        with:
          name: assurance-graph-executed-${{ github.run_number }}
          path: .context

      - uses: actions/download-artifact@v4
        with:
          name: openemr-evidence-${{ github.run_number }}

      # The pack the evidence job validated, by path. `find | head -1` picked whichever
      # .evidence file find listed first, and under pipefail failed the step with
      # "find: write error" once there were many.
      - name: Compute the coverage ribbon
        env:
          EVIDENCE_PACK: ${{ needs.evidence.outputs.pack }}
        run: |
          set -euo pipefail
          PACK="$EVIDENCE_PACK"
          if [ ! -e "$PACK" ]; then
            echo "::error title=Evidence pack missing::$PACK is not in the openemr-evidence artifact."
            exit 1
          fi
          echo "PACK=$PACK" >> "$GITHUB_ENV"
          kane-cli cover --from "$PACK" gaps --rollup strict            > coverage.txt || true
          kane-cli cover --from "$PACK" gaps --rollup strict --json     > coverage.json || true
          cat coverage.txt

      - name: Gate on proven coverage
        id: gate
        run: |
          python3 scripts/coverage_gate.py \
            --json coverage.json \
            --table coverage.txt \
            --pack "$PACK" \
            --threshold "$COVERAGE_THRESHOLD"

      - name: Comment the ribbon on the PR
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const ribbon = fs.existsSync('coverage.txt')
              ? fs.readFileSync('coverage.txt', 'utf8').trim()
              : '(no ribbon produced)';
            const gate = '${{ steps.gate.outcome }}' === 'success' ? '🟢 passed' : '🔴 below gate';
            const body = [
              '### OpenEMR patient portal assurance — designed × proven',
              '',
              `Coverage gate: ${gate} (threshold ${process.env.COVERAGE_THRESHOLD}%)`,
              `Use-cases: ${{ needs.assurance.outputs.usecase_count || 0 }} · Designed tests: ${{ needs.assurance.outputs.test_count || 0 }}`,
              '',
              '<details><summary>Coverage ribbon</summary>',
              '',
              '```',
              ribbon,
              '```',
              '',
              '</details>',
              '',
              `Evidence pack is attached to [run #${{ github.run_number }}](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}).`,
            ].join('\n');
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body,
            });

      - name: Publish the coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: openemr-coverage-${{ github.run_number }}
          path: |
            coverage.txt
            coverage.json
          retention-days: 30
          if-no-files-found: warn
